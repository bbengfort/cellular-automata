#!/usr/bin/env python

#from pyca.simple import Automata
from pyca.world import Automata
from pyca.animation import AutomataAnimation

#automata  = Automata(90, width=100, height=200, randstart=True)
#animation = AutomataAnimation(automata)
#animation.show()

automata  = Automata(width=100, height=100, randstart=False)
animation = AutomataAnimation(automata, interval=200)
animation.show()
